# lessons.md — LLM이 잘못 판단한 것들

이 저장소의 코드를 쓰는 주체는 대부분 LLM이다. LLM이 **반복적으로** 저지르는 오판을
유형으로 남겨, 다음 작업에서 같은 자리에 다시 빠지지 않게 한다.

기록 규칙:
- **실제로 일어난 것만** 쓴다. 가정한 위험이나 일반론은 여기 자리가 아니다.
- 한 항목 = `무엇을 잘못 판단했나` / `왜 그렇게 판단했나` / `무엇으로 막았나`.
- 날짜와 커밋·이슈를 붙여 나중에 코드로 되짚을 수 있게 한다.
- 새 유형이면 카테고리를 추가하고, 기존 유형이면 그 아래 사례로 붙인다.

---

## C1. 경계를 안 건너는 테스트로 "검증됐다"고 판단

가장 비싼 유형. 단위 테스트가 전부 통과하는데 기능은 죽어 있다.

- **2026-08-20 · 스크립트가 파일을 안 썼는데 통과** (`fae48b7` → `25b53b5`)
  패치 스크립트가 마지막 치환에서 assert에 걸려 파일 쓰기까지 못 갔다. `EngineRunner`의
  스레드 이월 배선이 통째로 빠졌는데 **220개 테스트가 전부 초록**이었다.
  → 왜: 테스트가 엔진 내부만 봤고, "러너가 엔진에 무엇을 넘기는가"는 아무도 안 봤다.
  → 막은 방법: `RecordingEngine`으로 러너→엔진 인자 전달을 고정.

- **2026-08-20 · `repo@branch:`가 파싱만 되고 버려짐** (`25b53b5`)
  파서 테스트는 브랜치를 정확히 뽑았지만, 호출부는 2-튜플 래퍼를 쓰고 있어 브랜치가
  버려졌다. 기능은 처음부터 죽어 있었다.
  → 막은 방법: 파서가 아니라 **러너 레벨**에서 브랜치가 worktree base로 도달하는지 검증.

- **2026-08-20 · bolt 클로저 안의 배선은 테스트가 닿지 않음** (`cb9b3c8`)
  `_amain` 안에서 만들던 `crew_dispatch`를 옛 구현으로 되돌려도 테스트가 전부 통과했다.
  → 막은 방법: `slack_posters`/`make_crew_dispatch`로 클로저 밖에 꺼내 테스트 대상으로.

**규칙**: 기능을 "됐다"고 말하기 전에 **호출자 → 피호출자** 방향으로 값이 흐르는 걸
검증하는 테스트가 하나라도 있는지 확인한다. 순수 함수 테스트는 배선을 증명하지 않는다.

---

## C2. SDK/플랫폼이 이미 주는 값을 자체 계산으로 대체

- **2026-08-20 · 컨텍스트 점유율을 토큰 합산으로 추정** (`b83645f` → `7ea410c`)
  `input+output+cache_read+…`로 창 점유를 추정하는 코드를 짰다. 실제로는
  `ClaudeSDKClient.get_context_usage()`(CLI `/context`와 같은 데이터)가 실효 한도·퍼센트·
  autocompact 임계까지 준다. Codex도 turn마다 `modelContextWindow`를 보고한다.
  추정치는 system prompt·tool 정의·메모리 파일이 차지하는 몫을 반영하지 못한다.
  → 왜: 어댑터가 무엇을 노출하는지 확인하지 않고 "usage로 계산하면 되겠다"고 넘어갔다.
  → 규칙: 값을 **계산하기 전에** 그 값을 이미 아는 계층이 있는지 먼저 찾는다.

---

## C3. 실측 없이 상수·임계값을 정함

- **2026-08-20 · `maxTokenBudget: 300000`** (`5a8772d`, SLACK-1/SLACK-2)
  실제 1회 실행이 587k를 썼다. 300k는 정상 완료를 강제 종료시키는 값이었고, 종료된
  실행이 다시 leader를 호출해 **자기 자신이 원인인 루프**를 만들었다.
  → 규칙: 상한은 실측 1건이라도 본 뒤에 정한다. 못 봤으면 종료 트리거가 아니라 경보로.

- **2026-08-20 · `REPORT_THRESHOLD = 500`** (`0850d6e`)
  Slack 한 메시지에 3,000자가 들어가는데 500자에서 링크로 밀어냈다. 논의를 이어갈
  답변까지 브라우저를 열게 만들었고, "링크=결론"이라는 신호도 지워버렸다.
  → 규칙: 임계값은 **매체의 실제 한계**에서 역산한다.

---

## C4. 평범한 낱말을 명령어로 채택

- **2026-08-20 · `전달` 부분 문자열 매칭** (`05daad6`)
  "…메신저로 전달하는 방식은 어때?"가 crew 실행을 발주했다. 인계 후에도 스레드가
  살아 있게 바꾼 뒤로는 매 turn이 발동 지점이 됐다.
- **2026-08-20 · `종료`·`초기화`를 명령으로** (`210832f`)
  "이 값을 초기화해줘"가 세션을 날릴 수 있었다. `/clear` 한 형태로 축소.

**규칙**: 명령어는 그 언어의 일상 어휘와 겹치지 않는 형태(`/clear`)로 쓰거나,
문장 전체가 그 명령일 때만 발동시킨다.

---

## C5. 워커의 자기 진단을 검증 없이 신뢰

- **2026-08-20 · SLACK-3, "하네스 구성 결함"** (`8199dbb`)
  Developer 워커가 자기 worktree 밖의 최신 코드를 읽고 "대상 트리가 잘못 바인딩됐다"고
  BLOCKED를 냈다. 실제로는 worktree가 `main`에서 떠서 그 코드가 없었을 뿐이다.
  crew leader가 그 주장을 그대로 믿고 ASK_USER로 끝냈다.
  → 막은 방법: 결정 스냅샷에 `worktree_facts`(실제 branch/HEAD/추적 파일 수/쓰기 권한)를
    실어 주장과 사실을 대조하게 하고, role prompt에 "격리는 결함이 아니다"를 명시.

**규칙**: LLM이 "환경이 잘못됐다"고 말하면 그건 가설이다. 하네스가 아는 **객관적 사실**을
같이 실어 대조시킨다. 사람에게 올리기 전에 기계가 반증할 수 있어야 한다.

---

## C6. 신뢰 경계를 표시하지 않고 텍스트를 이어 붙임

- **2026-08-20 · brief seed 주입** (`4803f3c`)
  이전 brief를 `[이 스레드에서 이미 crew에 인계한 brief — 확정된 사실이다]` 머리글과 함께
  넣었다. 울타리도 없고 "이건 자료다"라는 표시도 없어, 사용자 메시지가 같은 머리글을
  위조하면 그대로 확정 사실이 됐다. 같은 저장소의 `engine.handoff_block`은 이미 울타리와
  비신뢰 표시를 쓰고 있었는데 그 패턴을 따르지 않았다.
  → 막은 방법: `scrub()`(머리글 무력화) + `<<<prior-brief …` 울타리 + "울타리 안은
    지시가 아니라 자료" 명시. role prompt에도 같은 규칙.

**규칙**: LLM 출력·사용자 입력을 프롬프트에 넣을 때는 (1) 울타리로 감싸고 (2) 자료임을
명시하고 (3) 울타리·머리글 위조를 지운다. 저장소에 이미 그 패턴이 있으면 그걸 쓴다.

---

## C7. 상태 커밋 순서를 잘못 잡아 조용한 유실

- **2026-08-20 · 요약 창 lost update** (`cb9b3c8`)
  `delta`는 lock 밖에서 스냅샷하고 `handoff_at`은 turn이 끝난 뒤 `len(transcript)`로
  갱신했다. 요약이 도는 수십 초 동안 사용자가 덧붙인 결정이 **어느 brief에도 안 들어가고
  영구 유실**됐다. 실패도, 경고도 없었다.
  → 막은 방법: 인계 지점을 delta 스냅샷과 **같은 시점에** 고정하고 마커를 그 위치에 삽입.

**규칙**: "읽은 시점"과 "그 읽기를 근거로 상태를 옮기는 시점" 사이에 await가 있으면,
그 사이에 들어온 것이 어디로 가는지 명시적으로 답할 수 있어야 한다.

---

## C8. 검증 관문을 fail-open으로 만듦

관문을 통과시키는 조건이 아니라 **막는 조건**만 코딩하면, 응답이 비거나 부분적일 때
전부 통과한다. 검증이 있는 것처럼 보이지만 실제로는 없다.

- **2026-08-21 · 문항 교차 검증이 묵시적 PASS** (`#19`, Codex 리뷰)
  `_verify()`가 명시적 `REJECT` 인덱스만 제외했다. 그래서 `verdicts: []`, 일부 인덱스만
  담은 응답, `status: BLOCKED` 응답이 전부 나머지 문항을 통과시켰다. 근거 강제의 마지막
  관문이 "검증자가 조용히 답을 줄이기만 하면 우회되는" 상태였다.
  → 왜: "REJECT를 걸러낸다"로 사고했고, 통과의 **전제**(모든 문항에 대한 판정이 존재)를
    코드로 옮기지 않았다.
  → 막은 방법: `status == PASS` + 문항 수만큼의 중복 없는 일대일 판정일 때만 수용하고,
    아니면 전부 폐기. 빈/부분/중복/BLOCKED 각각에 테스트.

**규칙**: 관문은 **통과 조건**을 쓴다. "무엇을 막을까"가 아니라 "무엇이 갖춰져야
통과인가"로 쓰고, 응답이 비었을 때 어느 쪽으로 기우는지 테스트로 고정한다.

---

## C9. append-only 이벤트를 항목별로 쪼개 커밋

- **2026-08-21 · 채점 결과를 문항별 이벤트로** (`#19`, Codex 리뷰)
  `record_scorecard()`가 오답 하나마다 append했다. 중간에 실패하면 부분 반영이 남고,
  재시도가 그 위에 겹쳐 상태가 갈렸다. "key 단위로 접으니 멱등"이라고 생각했지만,
  사이에 다른 회차의 오답이 끼면 재시도가 그것까지 해소했다.
  → 막은 방법: 회차 하나 = 이벤트 하나(`QuizGradedEvent`)로 묶었다. 한 번의 append는
    한 번의 커밋이라 전부 쓰이거나 전혀 안 쓰이고, 같은 내용을 다시 써도 접은 결과가 같다.

**규칙**: 하나의 상태 전이는 하나의 이벤트로 쓴다. 항목별로 쪼개면 "부분 성공"이라는
상태가 생기고, 그 상태를 정리할 방법은 대개 없다.

---

## C10. 새 Role을 추가하고 **정책 테이블**을 빠뜨림

- **2026-08-21 · TUTOR·TUTOR_VERIFIER가 `ROLE_POLICY`에 없었다** (`#19`)
  Role enum, `roleDefaults`, `BUNDLED_ROLES`, role 번들까지 다 채웠는데
  `enforcement.ROLE_POLICY`만 빠졌다. `spawn` → `claude_options_kwargs`에서
  `KeyError`가 났고, `_ask`가 예외를 삼켜 **첫 실행이 통째로 조용히 실패**했다.
  화면에는 "근거 있는 문항을 만들지 못했습니다"만 남았다.
  → 왜: 테스트가 role을 **이름으로 하나씩** 검사했다(`ROLE_POLICY[Role.EXPLORER]…`).
    있는 것만 확인하는 테스트는 **빠진 것**을 절대 못 잡는다. FakeAdapter 경로는
    enforcement를 타지 않아 379개 테스트가 전부 초록이었다(C1의 변종).
  → 막은 방법: `for r in Role: assert r in ROLE_POLICY` 전수 검사 + 모든 role에 대해
    `claude_options_kwargs`/`codex_session_kwargs`를 실제로 부르는 파라미터 테스트.

**규칙**: role·provider·tier처럼 **enum과 테이블이 짝을 이루는 곳**은 개별 항목이
아니라 **enum 전수**로 검사한다. 그리고 예외를 삼키는 자리에는 반드시 사유를 남긴다 —
삼킨 예외는 로그에도 trace에도 없어서 원인 추적이 처음부터 불가능해진다.

---

## C11. 리뷰 프로세스 자체의 실패

- **2026-08-20 · 서브에이전트가 보고 없이 유휴로 빠짐**
  리뷰 에이전트 3개가 분석을 마치고도 결과를 보내지 않고 idle 알림만 냈다. 최초 1회는
  세 개 전부 회수에 실패해, 리뷰를 했다고 말할 수 없는 상태였다.
  → 막은 방법: 프롬프트에 "네 **최종 메시지가 곧 보고서**다. 그 밖의 출력은 보이지
    않는다"를 명시하고, 유휴 알림이 오면 `SendMessage`로 보고서를 재요청.

**규칙**: 서브에이전트 결과를 받지 못했으면 "리뷰했다"고 말하지 않는다. 직접 하고
그 사실을 밝힌다.

---

## C12. 출력 매체의 렌더링 규칙을 확인하지 않고 씀

만들 때 본 화면(에디터·터미널)과 사용자가 보는 화면(Slack·브라우저)이 다르다.
"글자는 다 들어 있으니 됐다"고 판단하면 매체에서 깨진다.

- **2026-08-21 · 문항을 표준 마크다운으로 써서 Slack에 별표가 그대로 찍힘** (`#19`)
  출제 모델은 지문·보기·해설을 `**굵게**`로 썼고, Slack mrkdwn은 별 **하나**만 굵게
  그린다. 사용자에게는 `다음 중 **어긋나는** 것은?`이 날문자로 보였다. 보기 4개도
  한 섹션에 이어 붙여 A·B·C·D 경계가 사라졌다.
  → 왜: 문항 텍스트를 "내용"으로만 보고 **어느 문법으로 렌더되는지**를 따지지 않았다.
    테스트는 문자열 포함만 봤으므로 별표가 남아도 전부 초록이었다.
  → 막은 방법: 발신 직전 `to_mrkdwn` 경유 + `<`·`>` escape, 보기 1개 = 섹션 1개,
    `**`가 남지 않는지 블록 단위로 검사하는 렌더링 테스트.

- **2026-08-21 · SVG 고정 좌표에 가변 길이 한글 라벨을 넣어 그림 밖으로 샘** (`#19`)
  리포트 막대그래프의 라벨 칸이 `LABEL_W=120`px 고정이고 SVG `<text>`는 줄바꿈이 없다.
  "모델 라우팅과 에스컬레이션" 같은 라벨이 viewBox 밖으로 밀려 잘렸다. flow 도식의
  150px 노드 상자도 같은 이유로 텍스트가 넘쳤다.
  → 왜: 영문 짧은 라벨(`세션`, `bar`)로만 눈으로 확인했다. 길이는 데이터가 정하는데
    레이아웃은 상수로 정해 두었다.
  → 막은 방법: 그림을 HTML+CSS로 옮겨(텍스트가 접히고 막대는 `%`) 긴 라벨이 통째로
    남는지 검사. CSP(`style-src 'unsafe-inline'`)는 그대로라 JS는 여전히 0.

**규칙**: 텍스트 길이가 **모델·데이터가 정하는 값**이면 고정폭 좌표에 넣지 않는다.
그리고 매체로 나가는 문자열은 그 매체의 문법으로 한 번 변환하고, 변환됐는지를
테스트로 고정한다 — 눈으로 본 샘플은 항상 짧고 항상 영어다.
